# Email Triage RL Environment — Python 3.11
# ==========================================
# IMPORTANT: openenv-core must be installed for spec compliance
openenv-core

# FastAPI server
fastapi>=0.104.0
uvicorn >= 0.24.0

# Data validation — Pydantic v1 (no Rust compile dependency)
pydantic >=1.9,<3

# HTTP client (used by client.py)
requests==2.31.0

# LLM — OpenAI client (MANDATORY per competition spec)
openai>=1.58.0

# Environment variables
python-dotenv==1.0.1

# Testing
pytest==8.2.0
httpx==0.27.0